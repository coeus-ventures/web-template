import { describe, test, expect } from 'vitest';

describe('Math operations', () => {
  test('addition works', () => {
    expect(1 + 1).toBe(2);
  });

  test('multiplication works', () => {
    expect(2 * 3).toBe(6);
  });
});
